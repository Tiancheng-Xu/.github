{
  "name": "tiancheng-xu-repository-policy",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:policy": "node --test tests/repository-policy.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
